import "./index.css";

// Swiper CSS - must be in globals for Webflow bundling
// @ts-expect-error - CSS imports don't have type declarations
import "swiper/css";
// @ts-expect-error - CSS imports don't have type declarations
import "swiper/css/pagination";